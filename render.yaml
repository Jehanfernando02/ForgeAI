services:
  - type: web
    name: ForgeAI
    env: python
    buildCommand: "pip install -r requirements.txt"
    startCommand: "PYTHONPATH=. gunicorn backend.app:app"
    envVars:
      - key: PYTHON_VERSION
        value: 3.11.9
      - key: PORT
        value: 5001
